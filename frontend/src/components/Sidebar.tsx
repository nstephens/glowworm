import React from 'react';
import { Sidebar as NewSidebar } from './layout/Sidebar';

export function Sidebar() {
  // This component is now deprecated in favor of the new layout system
  // The new Navigation component handles the sidebar internally
  return null;
}