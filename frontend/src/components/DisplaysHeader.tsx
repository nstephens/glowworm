import React from 'react';

export const DisplaysHeader: React.FC = () => {
  return (
    <div className="animate-fade-in-up">
      {/* Clean header without gradient accent or redundant text */}
    </div>
  );
};

export default DisplaysHeader;
