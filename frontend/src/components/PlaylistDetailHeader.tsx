import React from 'react';

export const PlaylistDetailHeader: React.FC = () => {
  return (
    <div className="animate-fade-in-up">
      {/* Clean header without gradient accent or redundant elements */}
    </div>
  );
};

export default PlaylistDetailHeader;
